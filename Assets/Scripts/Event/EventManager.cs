namespace Event {
    public class EventManager {
        // todo: start here
        public EventManager() {
            eventList = new EventList(64);
        }

        private EventList eventList;
    }
}
